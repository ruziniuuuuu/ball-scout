version: '3.8'

services:
  # PostgreSQL 数据库
  postgres:
    image: postgres:15-alpine
    container_name: soda-postgres
    environment:
      POSTGRES_DB: soda
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: soda123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/setup-database.sql:/docker-entrypoint-initdb.d/setup-database.sql
    networks:
      - soda-network
    restart: unless-stopped

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: soda-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - soda-network
    restart: unless-stopped

  # 速达足球后端 API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: soda-backend
    environment:
      - NODE_ENV=development
      - PORT=8000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=soda
      - DB_USER=postgres
      - DB_PASSWORD=soda123
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - JWT_SECRET=soda-jwt-secret-development-2024
      - CORS_ORIGIN=http://localhost:3000
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./logs:/app/logs
    depends_on:
      - postgres
      - redis
    networks:
      - soda-network
    restart: unless-stopped
    command: deno run --allow-net --allow-read --allow-env --watch mod.ts

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  soda-network:
    driver: bridge 